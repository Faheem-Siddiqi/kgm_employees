# Copy this file to .env and replace every value with your local settings.
# For packaged EXE installs, the app also checks config\.env beside the EXE.

KGM_DB_HOST=127.0.0.1
KGM_DB_PORT=3306
KGM_DB_NAME=kgm_ex_employees_demo
KGM_DB_USER=demo_user
KGM_DB_PASSWORD=change_me_db_password

KGM_ADMIN_USER=admin
KGM_ADMIN_PASSWORD=change_me_admin_password
FIELD_SETTINGS=change_me_field_settings_password

# True: use KGM_EMPLOYEE_STORAGE_DIR as a shared UNC/LAN employee folder.
# False: use KGM_EMPLOYEE_STORAGE_DIR as a local employee folder on this PC/app install.
KGM_EMPLOYEE_STORAGE_ON_SERVER=false

# One storage path only. Use a local relative/absolute path, or a UNC path when server mode is true.
# Home bulk upload also scans direct Employee-Code folders from this same path.
KGM_EMPLOYEE_STORAGE_DIR=resources/employees

# Shared UNC/LAN example:
# KGM_EMPLOYEE_STORAGE_ON_SERVER=true
# KGM_EMPLOYEE_STORAGE_DIR=\\192.168.2.93\employees

# Defaults shown in the shared storage reconnect dialog. Users can edit these before connecting.
KGM_EMPLOYEE_STORAGE_DEFAULT_SERVER=192.168.2.93
KGM_EMPLOYEE_STORAGE_DEFAULT_SHARE=employees
KGM_EMPLOYEE_STORAGE_DEFAULT_USERNAME=kgm534
KGM_EMPLOYEE_STORAGE_DEFAULT_PASSWORD=AM@12345

# Maximum prepared JPG/JPEG document/photo upload size in bytes. Larger JPG/JPEG files use the best JPEG quality that fits this limit.
# Commas are accepted, for example 614,400.
KGM_DOCUMENT_UPLOAD_MAX_BYTES=600000
# Compatible alias is also accepted when KGM_DOCUMENT_UPLOAD_MAX_BYTES is not set:
# DOCUMENT_UPLOAD_MAX_BYTES=600000

# Enable or disable compression during bulk import.
# When true, files larger than KGM_DOCUMENT_UPLOAD_MAX_BYTES are compressed before upload.
# When false, valid JPG/JPEG files are uploaded as-is without compression.
BULK_IMPORT_COMPRESSION=true

KGM_LONG_SERVICE_TIMEOUT_MINUTES=15
# Timeout in minutes for bulk document upload, Excel import, and Excel export services.
